
import Section from "@/components/Section"

const Contact = ({ id }: { id: string }) => {
  return (
    <Section
      id={id}
      title="Contact"
    >

    </Section>
  )
}

export default Contact